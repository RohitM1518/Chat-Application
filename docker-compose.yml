version: '3.8'

services:
  client:
    build: ./client
    ports:
      - "5173:5173"
    depends_on:
      - server
    env_file:
      - ./server/.env

  server:
    build: ./server
    ports:
      - "8000:8000"
    env_file:
      - ./server/.env
    